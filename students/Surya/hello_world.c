#include <stdio.h> //import functions
int main() {
	printf("hello world\n"); //include newline character to get new line, no print line function, just printf
	//all statements end in semicolons
}
//main returns an integer value, returns number if no work, 0 if works
//if everything works normal exit 0, error = > exit 1
//