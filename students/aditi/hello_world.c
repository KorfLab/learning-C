#include <stdio.h>
int main() {
	printf("hello world\n");
}

// once a program is compiled, it can be run multiple times

/* if you make a change to a program, 
you will need to recompile it, and rename it.
the default name is a.out.
the name can be the same as the name before 
ie, if I'm debugging the program, greeting, I can rename it greeting
then you compile and run
*/

